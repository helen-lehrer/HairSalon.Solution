@{
  Layout = "_Layout";
}

@model HairSalon.Models.Client

<h3 class="jumbotron jumbotron-fluid" id="heading">Are you sure you want to delete this?</h3>

@Html.DisplayNameFor(model => model.Name): @Html.DisplayFor(model => model.Name)
@using (Html.BeginForm())
{
  <input type="submit" value="Delete" class="btn btn-outline-dark" />
}
@Html.ActionLink("Back to Client List", "Index", "Clients", null, new {@class = "btn btn-dark"})